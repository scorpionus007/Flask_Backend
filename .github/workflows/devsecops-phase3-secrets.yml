name: DevSecOps CI - Phase 3.1 (Secrets Detection)

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  secrets-scan:
    name: Secrets Scanning (Gitleaks)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2
        with:
          fail: true
